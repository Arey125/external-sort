char* singlethread_sort(char *, long long int); //Sort function for .txt files whitch size smaller what RAM value

void sort_file(int);

int compare(const void *, const void *); //Compare function for qsort() argument
